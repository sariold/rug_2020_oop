package nl.rug.oop.grapheditor.model;

public class GraphSaver {

    public static void saveGraphModel(GraphModel g) {
        int amtNodes = g.getNodes().size();
        int amtEdges = g.getEdges().size();
    }
}
